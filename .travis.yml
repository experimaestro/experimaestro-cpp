language: java
jdk:
  - oraclejdk8
sudo: false
cache:
  directories:
      - $HOME/.gradle
install:
  - sudo apt-get install swig
branches:
  only:
   - master
   - develop
   - /^release\/.*/

# Build steps
script:
  - ./travis.sh

