# Main language
language: java
jdk:
  - oraclejdk8

# Environment
dist: trusty # Use more recent Ubuntu
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - llvm-toolchain-precise-3.6

    packages:
      - swig
      - ubuntu-toolchain-r-test


# Use faster container-based builds
sudo: false

# Configuration
cache:
  directories:
      - $HOME/.gradle
branches:
  only:
   - master
   - develop
   - /^release\/.*/

# Build steps
script:
  - ./travis.sh

